---
title: "Untitled"
output: html_document
---

```{r setup, include=FALSE}
library(Seurat)
library(tidyverse)
library(here)
```

```{r}
pathway_subset <- readRDS(here("explore/RDS/Fig4_pathway_subset.RDS"))
```

```{r}
pathway <- subset(pathway_subset, subset = Diagnosis == "COVID" | Diagnosis == "IPF")
```


```{r}
DefaultAssay(pathway) <- "RNA"

features_TGF <-c(
'ID2',
'ZFYVE16',
'THBS1',
'ROCK1',
'SKP1',
'RBX1',
'PPP2R1A',
'ID3',
'ID1',
'TGFBR2'
)

#VlnPlot(pathway, features = features_TGF, group.by = "cell_type_submain", sort = TRUE, pt.size = 0, split.by = "Diagnosis")

```


```{r}
features_WNT1 <- c(
'CTNNB1',
'PPP2R5C',
'CHP1',
'PRKCB',
'PRKACA',
'CCND1',
'RUVBL1'
)

#VlnPlot(pathway, features = features_TGF, group.by = "cell_type_submain", sort = TRUE, pt.size = 0, split.by = "Diagnosis")

```









